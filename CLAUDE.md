# ProfitLens — Workflow & Branch Rules

> **اقرأ ده الأول في أي جلسة.** القواعد دي ملزِمة لأي شغل في الـ repo.

## قواعد البرانشات (Git Workflow)

ترتيب البرانشات:

```
session branch (claude/...)  ──►  profitlens-testing  ──►  main (production)
        شغل الجلسة                    التكامل/الاختبار          الإنتاج
```

### القواعد الإلزامية

1. **كل الشغل والتعديلات هدفها النهائي برانش `profitlens-testing`.**
   - لو الجلسة بتفرض برانش مؤقت باسم `claude/...`، اشتغل عليه عادي،
     لكن في الآخر **لازم** التغييرات تتزامن/تتدمج مع `profitlens-testing`.
   - يعني `profitlens-testing` هو الـ integration branch لكل الميزات.

2. **`main` = production. ممنوع نهائيًا الـ push أو الـ merge المباشر عليه.**
   - `main` بيتحدّث **فقط** عن طريق merge من `profitlens-testing`،
     وده بيحصل **بعد مراجعة يدوية من صاحب المشروع**.
   - متعملش merge لـ `main` من نفسك أبدًا إلا لو المستخدم طلب ده **صراحةً** في الجلسة.

3. **متعملش Pull Request إلا لو المستخدم طلب صراحةً.**

4. **تنظيف البرانشات:** برانشات `claude/...` القديمة اللي اتدمجت خلاص في
   `profitlens-testing` يُفضّل تتمسح عشان الـ repo يفضل نظيف.

### الخلاصة في سطر
> اشتغل → زامن مع `profitlens-testing` → سيب `main` لحد ما المراجعة اليدوية
> توافق على الـ merge.
